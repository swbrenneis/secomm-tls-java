package org.secomm.tls.protocol.record;

/**
 * The TLS session abstracts a connection. It manages the state
 * of the connection and maintains the integrity of the record layer.
 */
public class TlsSession {

    private RecordLayer recordLayer;
}
